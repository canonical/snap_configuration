name: snap
on:
  push:
    branches:
      - howto/pull_configuration_from_a_server
  pull_request:
    branches:
      - howto/pull_configuration_from_a_server
  workflow_call:
    inputs:
       branch-name:
         required: false
         type: string
         default: ''

jobs:
  snap:
    uses:  canonical/robotics-actions-workflows/.github/workflows/snap.yaml@main
    with:
      git-ref: ${{ inputs.branch-name == '' && github.ref || inputs.branch-name }}
